# M-T428-V4 Teaching Portal Source

Synthetic, intentionally incomplete source-state fixture. It contains dated facts and prior-state artifacts for a later implementation/reconciliation workflow; it is not a finished release.

Start with `docs/` and the linked issue records. Do not treat any stated integration as enabled unless configuration and a live test independently confirm it.